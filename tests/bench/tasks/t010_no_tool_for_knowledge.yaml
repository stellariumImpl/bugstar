id: t010_no_tool_for_knowledge
description: 纯知识问答不应调用 terminal（避免 agent 什么都想跑一下）
prompt: |
  Python 里 list 和 tuple 的主要区别是什么？用一段话简要说明.
timeout_s: 30
checks:
  - type: tool_not_called
    name: terminal
  - type: reply_contains
    needles:
      - "可变"
    case_insensitive: true
